- name: retrive secrets
  hosts: localhost
  tasks:
    - debug:
        msg: "{{ lookup('hashi_vault', 'secret=test/MYPASSS token={{ vault_token}} url=https://vault-internal.rdevopsb80.online:8200 validate_certs=False')}}"

